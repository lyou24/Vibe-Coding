services:
  - type: web
    name: ongeki-recommend-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_PASSCODE
        value: "123123"
